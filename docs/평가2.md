add/list/search/summary/export/import/update/delete가 요구사항대로 동작하는가?
결과: Pass

사유:

실행 증빙 섹션(3.1, 3.3, 3.4, 3.5)에서 해당 명령어들이 정상적으로 동작하는 것을 로그를 통해 확인 가능함
프로그램 재실행 후에도 거래/카테고리/예산 데이터가 유지되는가? (저장 파일 3개 이상)
결과: Pass

사유:

아키텍처 설명 및 로직 분리 설명에서 **파일 I/O 기반 스토리지(JSONL, CSV 등)**를 활용하여 데이터를 영속화하고 있음을 증명함
category add/list/remove가 정상 동작하는가? (삭제 시 사용 중인 카테고리 처리 포함)
결과: Pass

사유:

실행 증빙 섹션(3.2)에서 카테고리 조회 및 삭제 시도 시 사용 중인 경우 방어 로직이 정상 작동함을 보여줌
budget set이 저장되며, summary에서 예산 사용률/초과 여부가 출력되는가?
결과: Pass

사유:

실행 증빙 섹션(3.3)에서 예산 설정 성공 메시지 및 summary 조회 시 사용률(150.0%)과 초과 경고 메시지가 출력됨을 확인함
import/export가 명시된 CSV 스키마(UTF-8, 헤더, 컬럼)로 동작하는가?
결과: Pass

사유:

실행 증빙 섹션(3.4)에서 요구된 스키마(date, type, category, amount, memo, tags) 형태의 CSV 파일 생성 및 읽기가 정상 동작함을 확인함
잘못된 입력/파일 오류에서 스택트레이스 없이 오류 메시지와 해결 힌트를 출력하는가?
결과: Pass

사유:

에러 처리 전략(Q3) 및 실행 증빙(3.2, 3.5)에서 스택트레이스 대신 친절한 원인 및 힌트 메시지가 출력됨을 확인함
오류 상황에서 종료 코드가 0이 아님을 확인할 수 있는가?
결과: Pass

사유:

에러 처리 데코레이터(Q3) 설명에서 sys.exit(1)로 에러 코드를 반환한다고 명확히 기술함
코드가 3개 이상 모듈로 분리되어 있고, 각 모듈의 책임을 “어떻게” 나눴는지 설명할 수 있는가?
결과: Pass

사유:

1.1 섹션에서 models.py, storage.py, services.py/io_service.py, main.py 등으로 분리하고 각 역할을 상세히 설명함
최소 2개 이상의 클래스에 부여한 책임 경계를 “어떻게” 정했는지 설명할 수 있는가?
결과: Pass

사유:

1.2 섹션에서 Transaction(상태/무결성 검증)과 TransactionService(행동/비즈니스 로직) 클래스의 책임을 명확히 구분하여 설명함
파일 기반 update/delete를 “어떻게” 안전하게 처리했는지 설명할 수 있는가?
결과: Pass

사유:

Q1에서 tempfile과 os.replace를 활용한 원자적 덮어쓰기(Atomic Rewrite) 패턴을 상세히 설명함
list/search를 제너레이터로 스트리밍 처리한 방식을 “어떻게” 구현했고, “왜” 유리한지 설명할 수 있는가?
결과: Pass

사유:

Q2에서 yield 키워드 활용 및 메모리 O(1) 유지를 이유로 스트리밍 방식의 이점을 명확히 설명함
데코레이터로 분리한 공통 기능이 무엇이며, “왜” 분리가 필요했는지 설명할 수 있는가?
결과: Pass

사유:

Q3에서 @error_handler, @time_logger 데코레이터를 통해 비즈니스 로직과 예외 처리 등의 관심사를 분리했다고 설명함
타입 힌트를 적용해 얻는 이점을 실제 코드 예로 “어떻게” 확인했고 “왜” 도움이 되는지 설명할 수 있는가?
결과: Pass

사유:

Q4에서 search 함수의 인자 및 반환값 타입 힌트 예시를 통해 런타임 에러 사전 방지 및 명세서 역할을 설명함
JSONL과 CSV 중 선택한 저장 포맷의 장단점을 비교하고, “왜” 그 포맷을 택했는지 근거를 말할 수 있는가?
결과: Pass

사유:

Q5에서 배열 데이터(tags) 처리 시 파싱 오버헤드를 줄이기 위해 JSONL을 선택했다고 장단점 비교와 함께 설명함
거래가 10만 건으로 늘어난다면, 현재 구조에서 병목이 어디이며 “어떻게” 개선할지 설명할 수 있는가?
결과: Pass

사유:

Q6에서 update/delete 시 전체 파일 재기록으로 인한 O(N) 비용을 병목으로 짚고, 파일 분할 또는 SQLite 도입을 대안으로 제시함
import CSV에 일부 깨진 행이 섞이면, “어떻게” 처리해 사용자 신뢰를 지킬지(부분 성공/롤백/리포트) 설명할 수 있는가?
결과: Pass

사유:

Q7에서 행 단위 try-except 블록을 활용한 부분 성공(Partial Success) 및 리포트 전략을 구체적으로 설명함
